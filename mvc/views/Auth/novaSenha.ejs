<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alterar Senha - SGFIC</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            overflow-y: auto;
            padding-left: 72px; /* Espaço para a nav recolhida (um pouco menor ) */
            transition: padding-left 0.3s ease-in-out;
        }
        main {
            flex-grow: 1; 
        }
    </style>
</head>
<body class="bg-gray-50 font-sans">

    <main class="p-6 sm:p-8 lg:p-10 max-w-4xl mx-auto w-full">
        <form action="/usuarios/mudar-senha" method="POST" id="changePasswordForm" class="flex flex-col h-full">
            
            <div class="flex-grow space-y-6">
                <div class="mb-1">
                    <h1 class="text-3xl font-bold text-gray-900">Alterar Senha</h1>
                    <p class="text-gray-600 mt-2">
                        Olá, <span class="font-medium"><%= usuario.nif %></span>. Como é seu primeiro acesso, você precisa alterar sua senha.
                    </p>
                </div>

                <% if (erro) { %>
                    <div class="bg-red-100 border border-red-300 text-red-700 p-4 rounded-md mb-4">
                        <div class="flex items-center">
                            <i class="fas fa-exclamation-triangle mr-2"></i>
                            <span><%= erro %></span>
                        </div>
                    </div>
                <% } %>

                <!-- Seção: Credenciais de Acesso -->
                <section>
                    <h2 class="text-xl font-bold text-gray-900 mb-3 pb-2 border-b">Nova Senha</h2>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-4">
                        <div>
                            <label for="nova_senha" class="flex items-center gap-2 block text-sm font-medium text-gray-700 mb-1">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-gray-600">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 10-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />
                                </svg>
                                Nova Senha
                            </label>
                            <input type="password" name="nova_senha" id="nova_senha" class="w-full p-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500" placeholder="Digite sua nova senha" required>
                        </div>
                        <div>
                            <label for="confirmar_senha" class="flex items-center gap-2 block text-sm font-medium text-gray-700 mb-1">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-gray-600">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                Confirmar Senha
                            </label>
                            <input type="password" name="confirmar_senha" id="confirmar_senha" class="w-full p-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500" placeholder="Confirme sua senha" required>
                        </div>
                    </div>
                </section>
            </div>

            <!-- Botões -->
            <div class="mt-auto pt-6 flex justify-between items-center flex-shrink-0">
                <a href="/" class="text-sm font-medium text-gray-600 hover:text-gray-900 py-2 px-4 rounded-md hover:bg-gray-200 transition-colors">
                    <i class="fa-solid fa-arrow-left mr-2"></i> Voltar
                </a>
                <button type="submit" class="bg-blue-600 text-white font-semibold py-2 px-5 rounded-md hover:bg-blue-700 transition-colors shadow-sm">
                    <i class="fas fa-key mr-2"></i>
                    Alterar Senha
                </button>
            </div>
        </form>
    </main>
</body>
</html>
